apiVersion: 1

policies:
  - orgId: 1
    receiver: slack-live-translate-alerts
    group_by:
      - grafana_folder
      - alertname
      - severity
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
